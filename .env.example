# Copy this file to .env and fill in your key.
# .env is gitignored — never commit it.
#
# The key is embedded at build time into the service-worker bundle only.
# Do NOT reference VITE_AI_API_KEY in popup.ts or content-script.ts.

VITE_AI_API_KEY=your_api_key_here
